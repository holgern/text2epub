# On EPUB

An EPUB is a ZIP archive of XHTML, metadata, and assets.

The builder writes a deterministic package by default, so two builds from the
same input produce byte-identical output.

See the [EPUB specification](https://www.w3.org/TR/epub-33/) for details.
