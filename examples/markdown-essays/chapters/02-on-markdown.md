# On Markdown

Markdown is a plain-text format that maps cleanly onto HTML.

## A table of features

| Feature     | Supported |
| ----------- | --------- |
| Headings    | yes       |
| Tables      | yes       |
| Code blocks | yes       |

## A code block

```python
from text2epub import create_epub_from_markdown
```

> Raw HTML is disabled, so Markdown stays the single source of truth.
